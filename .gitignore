node_modules
dist
dist-ssr
coverage
.vite
*.log
.DS_Store
.env
.env.local
.vercel
INTERVIEW_NOTES.md

# TypeScript build artifacts
tsconfig.tsbuildinfo
*.tsbuildinfo

# Compiled JS outputs (source is TypeScript — JS files in src/ are build artifacts)
src/**/*.js

# Claude local config
.claude/
